{
  "name": "tribunal",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "ASE-26 running project. Four advocates argue, three judges rule, the three rulings are never combined.",
  "scripts": {
    "deliberate": "node src/cli.js",
    "check": "node tools/repo-checks.js",
    "test": "node --test tests/*.test.js",
    "compare": "node tools/compare.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
