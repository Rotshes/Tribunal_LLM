# Turn NNN — <short title>

Date:
Branch / commit:

## 1. Intent
What this turn was really trying to achieve, and why it exists.
Not the request — the reason behind it. "Add validation" is a solution;
"catch bad input before it reaches the judges" is an intent.

## 2. Specification
The precise, checkable decisions given to the agent before it acted.
Every decision fixed here is one the agent did not guess.

## 3. Context supplied
Which files, conventions, and prior lessons were placed in the agent's reach.
Note what was deliberately left out and why.

## 4. Plan
The agent's plan, and what I changed in it before approving.
If I approved it unchanged, say so — that is also a record.

## 5. Execution
What the agent actually did. Files touched. Anything it did that the plan
did not mention.

## 6. Verification
How the output was checked against the specification, criterion by criterion.

| Criterion | Method | Result |
|---|---|---|
| | | |

**What I did not verify:** state it plainly.

## 7. Outcome
What is now locked, what is still open, and what the next turn should take up.

### Correction issued this turn
If I corrected the agent, the rule that correction became, and where it now lives
(CLAUDE.md, a rule file, or the pitfalls list). A correction not written down is
a correction that will be needed again.
